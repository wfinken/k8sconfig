apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: dns-critical
value: 1000000
globalDefault: false
description: "High priority for cluster DNS; scheduled before other workloads after restart."
